class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __str__(self):
        # What gets shown when you print the object
        return f"{self.name} ({self.age})"

p1 = Person("Amina", 20)
print(p1)  # Output: Amina (20)